const StripePayments = ({
  priceInCent,
  orderId,
  clientSecret,
}: {
  priceInCent: number;
  orderId: string;
  clientSecret: string;
}) => {
  return <div>Stripe Form</div>;
};

export default StripePayments;
